# 开发与交付

## 开始一个功能

1. 先读 [`docs/development-guide.md`](docs/development-guide.md)，确认文档职责、当前状态和切片门槛。
2. 在 `docs/xiuxian3-design.md` 确认玩法方向、世界术语和所属领域。
3. 为用户流程、命令/交互、Web（如有）、数据、事务、任务、配置、适配器差异、
  测试和回滚写功能文档。
4. 定义领域不变量、错误类别和 operation 幂等策略。
5. 先写领域/应用测试，再接入命令、按钮和 Web。
6. 更新 manifest、迁移、观测和运行手册，并执行 `docs/testing.md` 的门槛。

## 提交边界

- 一个提交尽量只包含一个可回滚的垂直切片或一个基础设施能力。
- 不提交 `.env`、数据库、运行日志、备份、真实用户数据、token、外部响应和
  生成的媒体缓存。
- 不从旧项目复制业务实现、玩法或数据模型；适配器兼容层按
  `docs/reference-sources.md` 记录来源后可以复用，新规则必须在本项目文档中明确。
- 修改命令/URL/数据迁移必须同步更新对应设计文档（若改变长期决策则记录 ADR）。

## 代码审查问题

- 领域是否依赖平台或数据库实现？
- 资产变化是否有单一事务、operation、流水和失败回滚？
- 重试、并发、冷却、过期和定时任务是否明确？
- OneBot/QQ/Web 是否都走统一用例和消息投递？
- 测试是否使用隔离目录、确定性时钟/随机和假凭据？
- 关闭 feature 或恢复备份是否可验证？
