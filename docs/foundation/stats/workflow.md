# 属性域：计算生命周期

```text
dirty -> building -> ready
                  \-> invalid
ready -> invalid
```

境界、道途、装备、状态或规则变化时标记 `dirty`。`building` 只在内存中构建；校验通过进入不可变 `ready` 快照；引用缺失、数值越界或版本不可用进入 `invalid`，禁止用于资产结算。

加成分为基础、道途、构筑、环境、临时五个乘区。同一乘区先相加再相乘。运行时使用定点整数，公式尾部统一舍入。