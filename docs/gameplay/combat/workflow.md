# 战斗域：行动与状态

```text
created -> running -> won/lost/escaped/expired -> settled
```

自动回合顺序：服务端根据快照和策略选择行动 -> 目标合法性 -> 消耗预览 -> 命中/抵抗
-> 主效果 -> 状态效果 -> 持续效果 -> 胜负判断。客户端不参与回合选择。

状态分为增益、减益、持续、控制和道途状态。每个状态定义叠加、刷新、覆盖、免疫、
驱散和结束条件。行动超时按战斗定义自动防御、跳过、判负或结束；断线不会暂停自动战斗。
